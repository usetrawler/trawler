export default function Home() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-3xl flex-col justify-center gap-6 px-4">
      <p className="font-mono text-xs tracking-[0.2em] text-action uppercase">Trawler</p>
      <h1 className="text-5xl font-bold tracking-tight">From a URL to real user feedback.</h1>
      <p className="text-lg text-muted">Sign-in arrives with the next change.</p>
    </main>
  );
}
